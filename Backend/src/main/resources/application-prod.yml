# =============================================
# TravelMate AI - Production Profile
# =============================================
# Activated with: --spring.profiles.active=prod

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
    com.travelmateai: INFO
    org.springframework.security: WARN
